name: CD - Deploy no GitHub Pages com Notificações

on:
  push:
    branches: [ main ]

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    
    steps:
    - name: 📥 Checkout do código
      uses: actions/checkout@v4

    - name: 🔍 Verificar index.html
      run: |
        if [ ! -f "index.html" ]; then
          echo "❌ ERRO: Arquivo index.html não encontrado!"
          exit 1
        fi
        echo "✅ index.html validado!"

    - name: ⚙️ Configurar Pages
      uses: actions/configure-pages@v4

    - name: 📦 Upload dos arquivos
      uses: actions/upload-pages-artifact@v3
      with:
        path: '.'

    - name: 🚀 Publicar no GitHub Pages
      id: deployment
      uses: actions/deploy-pages@v4

    - name: 🎉 Deploy concluído com sucesso!
      if: success()
      run: |
        echo "✅ Deploy realizado com sucesso!"
        echo "📱 Site publicado em: ${{ steps.deployment.outputs.page_url }}"
        echo "🎊 Parabéns! O site está no ar!"

    - name: 📬 Enviar notificação de sucesso por Discord
      if: success()
      uses: sarisia/actions-status-discord@v1
      with:
        webhook_url: ${{ secrets.DISCORD_WEBHOOK }}
        status: ${{ job.status }}
        title: "✅ Deploy Bem-sucedido!"
        description: |
          🚀 Seu portfólio foi publicado com sucesso!
          📱 Acesse em: ${{ steps.deployment.outputs.page_url }}
          ✨ Autor: Erick Torres

    - name: 📧 Enviar notificação de sucesso por Email
      if: success()
      uses: dawidd6/action-send-mail@v3
      with:
        server_address: ${{ secrets.MAIL_SERVER }}
        server_port: ${{ secrets.MAIL_PORT }}
        username: ${{ secrets.MAIL_USERNAME }}
        password: ${{ secrets.MAIL_PASSWORD }}
        subject: "✅ Deploy do Portfólio Bem-sucedido"
        to: ${{ secrets.MAIL_TO }}
        from: ${{ secrets.MAIL_FROM }}
        body: |
          Olá Erick,
          
          🎉 Seu portfólio foi publicado com sucesso no GitHub Pages!
          
          📱 Acesse aqui: ${{ steps.deployment.outputs.page_url }}
          
          Parabéns pela automação bem-sucedida!
          
          ---
          Mensagem gerada automaticamente pelo GitHub Actions

    - name: ❌ Deploy falhou!
      if: failure()
      run: |
        echo "ERRO: Deploy falhou!"
        echo "⚠️ Verifique os logs para mais detalhes."
        exit 1

    - name: 📬 Enviar notificação de falha por Discord
      if: failure()
      uses: sarisia/actions-status-discord@v1
      with:
        webhook_url: ${{ secrets.DISCORD_WEBHOOK }}
        status: ${{ job.status }}
        title: "❌ Deploy Falhou!"
        description: |
          ⚠️ Ocorreu um erro durante o deploy do portfólio.
          🔍 Verifique os logs no GitHub Actions.
          📍 Repository: ${{ github.repository }}

    - name: 📧 Enviar notificação de falha por Email
      if: failure()
      uses: dawidd6/action-send-mail@v3
      with:
        server_address: ${{ secrets.MAIL_SERVER }}
        server_port: ${{ secrets.MAIL_PORT }}
        username: ${{ secrets.MAIL_USERNAME }}
        password: ${{ secrets.MAIL_PASSWORD }}
        subject: "❌ Deploy do Portfólio Falhou"
        to: ${{ secrets.MAIL_TO }}
        from: ${{ secrets.MAIL_FROM }}
        body: |
          Olá Erick,
          
          ⚠️ Ocorreu um erro durante o deploy do portfólio!
          
          ❌ Status: ${{ job.status }}
          
          🔍 Verifique os logs em:
          ${{ github.server_url }}/${{ github.repository }}/actions
          
          ---
          Mensagem gerada automaticamente pelo GitHub Actions
